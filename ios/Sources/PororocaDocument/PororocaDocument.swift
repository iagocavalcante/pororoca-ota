/// PororocaDocument: screen document schema v1 types, strict codec and validator (contract C1).
/// Task 2 provides the schema and codec; Task 4 adds validation.
public enum PororocaDocument {
    public static let moduleName = "PororocaDocument"
}
